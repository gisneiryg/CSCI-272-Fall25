This folder contains practice codes and Assignment 1 for Week 1
#include <iostream> 

  using namespace std;

  double average (int scores []),i
nt size{
      int sum = 0;
      for ( int i = 0; i < size; i++){
          sim += scores[i]
      }
      return (double) sum/size
  int main() {
      int userinput;
      cout << "Enter the zise of the array: " << endl;
      cin >> userinput;

     // int scores[5] = {90, 85, 80, 95, 100} // on array of 5 elements
      int scores[userinput] =

      // loop thorugh the array
      }
      cout << "Average = " << average (scores, 5) << endl;

      return 0;

  }
